package pub.wii.cook.scala.parquet

import org.apache.parquet.hadoop.ParquetFileReader
import org.apache.parquet.hadoop.ParquetFileWriter

object TestParquet {
  def main(args: Array[String]): Unit = {
  }
}
