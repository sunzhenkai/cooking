spring:
  redis:
    database: 0
    lettuce:
      pool:
        min-idle: 5
        max-idle: 10
        max-active: 8
        max-wait: 1ms
      shutdown-timeout: 100ms
    host: 127.0.0.1
    port: 6379
#    cluster:
#      nodes: localhost:6379
    password:

test:
  password: secret:test-decrypt-passwd

